<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_today">

    <fragment
        android:id="@+id/nav_today"
        android:name="better.weather.ui.home.HomeFragment"
        android:label="@string/todays_weather"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/nav_forecast"
        android:name="better.weather.ui.dashboard.DashboardFragment"
        android:label="@string/forecast"
        tools:layout="@layout/fragment_dashboard" />

</navigation>